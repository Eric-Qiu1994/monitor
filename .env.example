# 必填：随机、足够长的共享 token；agent 与 monitor 必须相同。
MONITOR_TOKEN=change-this-to-a-long-random-token

# 默认 ICMP 探测目标；设为空字符串可关闭。
MONITOR_PING_TARGET=1.1.1.1
MONITOR_PING_COUNT=3

# 可选
MONITOR_PORT=8080
MONITOR_RETENTION_DAYS=7
MONITOR_MAX_ROWS_PER_SERVER=20000
MONITOR_CLEANUP_INTERVAL=600
